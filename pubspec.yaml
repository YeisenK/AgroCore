name: agrocore
description: Dashboard Agricultor (AgroCore)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^0.65.0     # estable con Flutter/Dart 2.x
  provider: ^6.0.5      # compatible con Dart 2.17–3.x
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
